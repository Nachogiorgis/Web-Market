#!/bin/sh

# Try to use node from PATH if available (normal case)
if command -v node >/dev/null 2>&1; then
  lint-staged
  exit $?
fi

# Fallback: use Node installed via nvm for this user
"/Users/nachogiorgis/.nvm/versions/node/v20.11.0/bin/node" node_modules/lint-staged/bin/lint-staged.js

